# Настройка подключения к Production API

## ✅ Что уже сделано:

1. ✅ Обновлен `src/api/client.ts` - использует `https://backof.onrender.com/api/v1` по умолчанию
2. ✅ Обновлен `vite.config.ts` - прокси настроен для dev режима

## 📝 Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=https://backof.onrender.com/api/v1
```

**Важно:** Файл `.env` должен быть в корне проекта (рядом с `package.json`)

## 🔄 Перезапустите dev сервер:

```bash
npm run dev
```

## 🧪 Проверка подключения:

### 1. Проверьте health check:

Откройте консоль браузера (F12) и выполните:

```javascript
fetch('https://backof.onrender.com/health')
  .then(r => r.json())
  .then(console.log);
```

Должно вернуть: `{status: "ok", environment: "production"}`

### 2. Проверьте загрузку данных:

После входа в систему, проверьте консоль:
- Должно быть: "✅ Загружено всего классов: 66"
- Должно быть: "✅ Загружено всего учителей: 66"

## 🔐 Аутентификация:

Используйте реальные учетные данные для входа. API работает на:
- **Base URL**: `https://backof.onrender.com/api/v1`
- **Swagger Docs**: `https://backof.onrender.com/docs`
- **Health Check**: `https://backof.onrender.com/health`

## ⚙️ Настройки:

### Dev режим (npm run dev):
- Использует прокси через Vite: `/api/v1` → `https://backof.onrender.com/api/v1`
- Или использует прямой URL из `.env` если указан `VITE_API_URL`

### Production режим (npm run build):
- Использует `VITE_API_URL` из `.env` или `https://backof.onrender.com/api/v1` по умолчанию

## 🆘 Если не работает:

1. **CORS ошибка**: Убедитесь, что URL фронтенда добавлен в CORS на Render
2. **401 Unauthorized**: Проверьте токен в localStorage
3. **Медленный ответ**: Первый запрос может занять 30-60 секунд (Render free план)

